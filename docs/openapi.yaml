openapi: 3.1.0
info:
  title: Credence API (Runtime Source of Truth)
  version: 0.1.0
  description: |
    This static file is intentionally minimal.

    The source of truth is generated at runtime from `src/openapi/spec.ts`
    and served by the application at:
    - `GET /api-docs/openapi.json`
    - `GET /api-docs` (Swagger UI)
paths: {}
